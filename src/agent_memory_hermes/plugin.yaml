name: agent_memory
version: 0.4.0
description: "SurrealDB Agent Memory — provenance-first, tri-temporal agent memory with semantic, lexical, graph and temporal recall."
pip_dependencies:
  - surrealdb[memory]>=3.0.0b8
hooks:
  - on_session_end
